# Examples

This folder holds small curated examples only:

- `sample-data/` for compact CSVs
- `sample-call-sheets/` for representative SVG/PNG drafting outputs
- `sample-stls/` for lightweight printable examples

Full generated histories should stay out of Git and be regenerated from the apps.
