# Tools

Reusable helpers gathered from the apps:

- `material-adapters/` - material database and Cosmosis CSV profile adapters.
- `relief-generator/` - standalone silhouette/cutout bas-relief STL helper.
- `shared-senders/` - sender scripts for external shaper streams.

Some tools also remain inside their app folders so each app can run standalone.
