#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
COMMAND_FILE="$(cd "$SCRIPT_DIR/../../.." && pwd)/Cosmosis Navigator Fresh Start.command"

exec /usr/bin/open -a Terminal "$COMMAND_FILE"
